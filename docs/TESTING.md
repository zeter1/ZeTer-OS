# Проверка ZeTer OS

Это единственный источник команд, постоянных smoke-тестов и ручных сценариев. Начинай с узкой проверки; после структурного JS/CSS-, resource-, data-, bridge-, docs- или checker-изменения выполняй полный strict-check.

## Единая строгая команда

```powershell
.\check_project.cmd --strict-node --no-pause
```

Windows-wrapper ищет Python в `ZETER_PYTHON`, `.venv`, `py`, `python` и runtime Codex. Node берётся из `ZETER_NODE`, `PATH` или runtime Codex. Указывай переменные окружения как полный путь к executable; пользовательские абсолютные пути не записывай в проект.

Успешный итог содержит только `0 warnings` и `0 failures`. Строгий режим проверяет структуру, документацию, Python/JS syntax, запускает весь постоянный JavaScript scenario smoke-suite, герметичный Python native-smoke и smoke сборщика релиза.

## Узкие команды

```powershell
node --check путь\к\файлу.js
python -m py_compile путь\к\файлу.py
node tools/run_smokes.js
node tools/smoke_boot_guard.js
node tools/smoke_shell_runtime.js
node tools/smoke_system_settings.js
node tools/smoke_calendar_runtime.js
node tools/smoke_state_migration.js
node tools/smoke_storage_runtime.js
node tools/smoke_security_protection.js
node tools/smoke_import_runtime.js
node tools/smoke_export_runtime.js
node tools/smoke_managed_file_runtime.js
node tools/smoke_shortcut_runtime.js
node tools/smoke_item_customization.js
node tools/smoke_help_content.js
python tools/smoke_managed_file_native.py
python tools/smoke_release_builder.py
python tools/update_docs.py --write
python tools/update_docs.py --check
python tools/find_owner.py "explorer preview"
python tools/find_owner.py createSearchController --json
```

Если прямой `python`/`node` отсутствует, используй найденный wrapper-ом runtime. Не отмечай команду успешной, если она фактически не выполнялась.

## Постоянные smoke-тесты

| Файл | Контракт |
|---|---|
| `smoke_boot_guard.js` | фатальная runtime-ошибка, boot-timeout, блокирующий storage-recovery с четырьмя безопасными действиями и запись в native-лог |
| `smoke_shell_runtime.js` | часы открывают календарь на текущем месяце, повторный клик закрывает его на текущем рабочем столе и не затрагивает окна других рабочих столов; применение оформления при boot не сохраняет state, а пользовательские quick-настройки сохраняют явно |
| `smoke_desktop_group_drag.js` | добавление и снятие отдельных значков через Ctrl+ЛКМ, обычное одиночное выделение, захват всей выбранной группы, единый pointer-delta, сохранение взаимного расположения, общие границы экрана и обход запрещённых панелей |
| `smoke_system_settings.js` | defaults/migration глобальных настроек, назначение/конфликты горячих клавиш, тихие часы и однократная доставка календарного напоминания |
| `smoke_calendar_runtime.js` | каждый день месяца показывает свою кнопку добавления, форма живёт в отдельном редакторе, сохранение и редактирование не дублируют событие |
| `smoke_explorer_navigation.js` | Windows-подобная навигация, synthetic desktop root, drag/drop fallback-зоны, сохранность desktop-координат в ordered view и ожидание подтверждённого удаления до очистки ссылок/перехода |
| `smoke_state_migration.js` | defaults, legacy migration, повторяемость, save/load, очистка старых записей удаления, точное предупреждение текущего удаления с recovery-удержанием payload и откат объекта при отказе записи |
| `smoke_storage_runtime.js` | native/IndexedDB records, явный обычный/аварийный результат, отклонение Promise при полном отказе, продолжение очереди после ошибки и блокировка записи до загрузки; успешная native-загрузка, настоящий первый запуск, ошибка доступа, повреждённый JSON, отсутствие boot-записей после ошибки и повторное чтение с разблокировкой |
| `smoke_monitor_runtime.js` | верхняя кнопка Монитора, модели CPU/RAM/диска, устойчивые пороги предупреждений, системная сводка, native/browser HTML, диагностический отчёт и асинхронное обновление короткой истории |
| `smoke_security_protection.js` | manifest и контрольные суммы ZIP, structured protection policy/journal, AES-GCM encrypt/decrypt и отказ при неверном пароле; отсутствие ложной текущей Корзины в Центре безопасности при сохранённой проверке legacy-записей; блокировка native-restore до замены state при отсутствующем payload, откат состояния без reload при отказе записи и передача ошибки полного native-сброса |
| `smoke_editor_ui.js` | rich-text helpers, DOCX-dispatch, ожидание Promise автосохранения, обычный/аварийный статус, ошибка записи и защита нового статуса от запоздалого результата |
| `smoke_import_runtime.js` | hydration assets, выбор папки и полный import action |
| `smoke_export_runtime.js` | asset I/O, ownership, readable entries, ZIP, CSV с импортом `;`/`,`, XLSX-листы и размеры, browser/native text/binary download с выбранной папкой, запись и scheduler |
| `smoke_managed_file_runtime.js` | безопасные managed-пути, inline-маркеры, перенос без дубля при drag/drop, копирование через Ctrl+C/Ctrl+V, подгонка ширины файла под ячейку и точный порядок chunked native-вызовов |
| `smoke_managed_file_native.py` | выбор развёрнутого/обычного окна из сохранённого state, герметичное включение/обновление/выключение пользовательского автозапуска через подменённый реестр, реальный read-only замер CPU/RAM/диска/uptime через native bridge, полная побайтовая копия больше одного chunk и атомарная публикация; уникальные temp-файлы при параллельной записи, очистка temp после искусственного `os.replace`-сбоя и сериализация параллельных add/delete точек; транзакционный `save_state`, неизменяемость restore points/backups при обычном удалении и сохранность прежнего state, recovery-снимков, managed-файла и item asset при искусственном отказе primary-записи; объединённое удержание payload текущим state, точками и JSON-backups; полный сброс/перезапуск, точный preflight отсутствующего или небезопасного пути, восстановление тех же байтов/значка/фона и GC после удаления последней recovery-ссылки; атомарная запись оформления, перенос всей `data`, сохранность канонического JSON и прежней Windows-копии при сбое её пересборки; герметичное открытие managed-файла, папок, Windows-пути и URL через подменённый платформенный адаптер |
| `smoke_release_builder.py` | одинаковый SHA-256 при повторной сборке, белый список runtime-файлов, отсутствие `data`/логов/служебных и тестовых файлов, проверка CRC и распаковки в путь с пробелами и кириллицей, сохранность предыдущего ZIP при ошибке |
| `smoke_shortcut_runtime.js` | допустимые Windows/URL/`zeter://` цели, отказ опасным схемам и относительным путям, нормализация state, пункты контекстного меню и автопревращение вставленного URL в ссылку |
| `smoke_item_customization.js` | нормализация цвета и переносимых `assetPath`, HTML своих значков/окон, изменение имени/цели ярлыка и пункты «Настройка папки/ярлыка» |
| `smoke_help_content.js` | полнота разделов справки, быстрый маршрут, поиск/навигационные контракты, реальные app-id, критические предупреждения и точное действие первого запуска |

Все файлы находятся в `tools/`; `run_smokes.js` запускает их последовательно из корня проекта. Одноразовый тест не сохраняй. Постоянный риск добавляй отдельным небольшим smoke либо в существующий профильный файл.

Для узкого smoke используй минимальные заглушки реальных зависимостей и проверь успех, пустой/граничный ввод, ожидаемую мутацию, error/fallback и callback-порядок action flow.

Strict-check запускает `smoke_managed_file_native.py` и `smoke_release_builder.py` отдельными Python-процессами через тот же интерпретатор, которым запущен checker. Для native-smoke установлен timeout 60 секунд, для release-smoke — 30 секунд; ненулевой код завершения или timeout считается общей ошибкой проверки.

## Что проверяет strict-check

- обязательные файлы, документы, Markdown-ссылки и контракт `AGENTS.md`;
- ресурсы `index.html`, порядок/граф core и PWA cache;
- один `window.ZETER_*` на core и ручную core-карту `CODEMAP.md`;
- app.js-якоря `CODEMAP.md`;
- manifest владельцев, его файлы/якоря, поисковый CLI и все generated navigation docs;
- JavaScript ↔ `NativeStorageApi`;
- чистую CSS-точку входа, существование/порядок CSS-карты и cache coverage;
- Python/JavaScript syntax, включая smoke-файлы;
- весь постоянный JavaScript scenario smoke-suite, профильный Python smoke managed-файлов и smoke сборщика релиза.

## Воспроизводимая UI-проверка

1. Запиши сценарий, режим `native`/`browser`, версию и ожидаемый результат.
2. Для native запусти `start_zeter_os.cmd`. Базовый размер окна — `1320×840`, узкий — минимальный `1024×640`.
3. Открой изменённый сценарий с обычными данными, пустым состоянием, длинным русским текстом и большим списком.
4. Проверь mouse, keyboard, touch-поведение при наличии, hover/focus/active/disabled, loading/error, scroll, clipping, overlap и z-index.
5. Если данные сохраняются, закрой приложение, запусти снова и повторно открой объект.
6. Для browser/PWA при необходимости запусти из корня `python -m http.server 8000 --directory app` и открой `http://127.0.0.1:8000/`; не используй реальные native-данные как фикстуру.
7. Сохраняй временные снимки как `work/ui-checks/YYYY-MM-DD/сценарий-normal.png` и `сценарий-narrow.png`. Папка игнорируется Git.

Если GUI не открывался, передай эти шаги как непроверенные, а не как пройденные.

## Ручная матрица

| Область | Минимальный сценарий |
|---|---|
| Запуск | Включить автозапуск на одном рабочем столе, перейти на другой и сверить общую галочку; перезапустить ZeTer OS, затем проверить появление программы после повторного входа в Windows и безопасное выключение автозапуска. Отдельно выбрать обычное/развёрнутое внешнее окно и стартовый рабочий стол, закрыть ZeTer OS, открыть повторно и сверить результат |
| Shell/окна | Пуск, taskbar, поиск; открыть, переместить, свернуть и развернуть окно |
| Значки рабочего стола | Удерживая Ctrl, поочерёдно выбрать разнесённые файл, папку и приложение; повторным Ctrl+ЛКМ снять один значок, обычным кликом оставить один; снова собрать группу, перетащить её за любой выбранный значок и сверить общее перемещение без изменения расстояний, сохранение выделения и координат после перезапуска |
| Проводник | Папки, tabs, address, preview, selection, drag/drop и download |
| Редакторы | Открыть, изменить, дождаться перехода статуса «Сохранение…» в «Автосохранено», переименовать и повторно открыть файл; в тестовой копии при недоступном хранилище проверить «Не сохранено», а не ложный успех |
| Удаление и legacy-state | Удалить объект после подтверждения, перезапустить и убедиться, что он не вернулся; в тестовой копии при недоступном хранилище проверить, что объект остаётся на месте и показано «Удаление не сохранено»; отдельно загрузить временный старый state с `deletedAt`/`TRASH_ROOT` и проверить миграцию и безопасную очистку |
| Задачи/календарь | CRUD, checklist/repeat, filter/view и reminder |
| Уведомления | Filter, read/delete, категории, тихие часы, напоминания задачи/календаря и переход к связанному объекту |
| Настройки/UI | Проверить «Сохранить сейчас», путь/status хранилища, общий автозапуск Windows и его browser-disabled состояние, восстановление окон, стартовый стол, размер текста, крупные элементы, контраст, уменьшение анимации, переназначение/отключение/сброс горячих клавиш, конфликт сочетаний и сохранение после перезапуска |
| Справка | На первом запуске нажать «Посмотреть возможности»; проверить быстрый маршрут, поиск с результатом и без него, навигацию, раскрытие тем, кнопки открытия приложений, прокрутку и узкое окно |
| Import/export | Обычный file import и полный JSON/ZIP round-trip |
| Portable-релиз | Запустить `build_release.cmd`, проверить появление `dist/ZeTer_OS_<версия>_portable.zip`, распаковать его и убедиться, что программа запускается, а папка `data` создаётся пустой только после первого запуска |
| Windows-копия заметок | Создать две одинаково названные заметки: одну на рабочем столе, другую внутри папки; после сохранения проверить, что обе лежат прямо в `data/Рабочие столы/<стол>/Заметки` как `Имя.docx` и `Имя (2).docx`, а вложенная папка не создана |
| Storage/bridge | Быстро сохранить несколько раз, перезапустить, открыть data folder и проверить log error; в тестовой копии искусственно заблокировать запись, применить точку и убедиться, что прежний state остаётся активным без reload; закрыть ZeTer OS, скопировать всю папку `data` в другую копию программы на другом пути, запустить её и сверить заметки, задачи, календарь, настройки и managed-файлы после повторного сохранения |
| Managed-файлы | Перетащить Windows-файл в позицию текста, в пустую нижнюю область и поочерёдно к левой/правой границе заметки или стикера; после повторного открытия проверить сохранение обеих координат; перенести внутренний ярлык drag/drop и убедиться, что исходный исчез, затем отдельно проверить копирование через Ctrl+C/Ctrl+V; в таблице переместить ярлык в разные части ячейки, вручную задать ему ширину, уменьшить ячейку до частичного скрытия и увеличить обратно без изменения размера ярлыка; затем проверить полную копию в `data/Файлы ZeTer OS`, открытие, перезапуск и физическое удаление вместе с последней ссылкой |
| Ярлыки и ссылки | На рабочем столе и в папке создать ярлыки на существующий Windows-файл, папку, `https://`-сайт и путь `zeter://item/...`, полученный через «Скопировать путь расположения»; открыть двойным кликом, перенести каждый ярлык в заметку/стикер/таблицу и обратно; удалить и убедиться, что внешняя цель не удалена; вставить URL в заметку и таблицу и открыть кликом |
| PWA/browser | IndexedDB, обновление offline-cache и повторный запуск |

## Перед сдачей

1. `node --check` изменённых JS и профильный smoke.
2. Поиск старых имён, дубликатов и всех затронутых selectors.
3. Generated docs: `--write`, затем `--check`.
4. Полный strict-check.
5. `git diff --check`, `git status --short`, `git diff --stat` и просмотр diff.
6. Фактический UI-сценарий либо точная передача непроверенных шагов.

В отчёте укажи команды, результаты, изменённые файлы, что проверено только по коду и оставшиеся риски.
